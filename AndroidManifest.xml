<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_helloworldpro">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <service
            android:name=".MyBindService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".chapter6.MyService"
            android:enabled="true"
            android:exported="true" />
        <activity android:name=".chapter9.DatabaseActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".chapter9.SharedPreferencesActivity"></activity>
        <activity android:name=".chapter6.MyBindServiceActivity" />
        <activity android:name=".chapter6.MyServiceActivity" />
        <activity android:name=".CarsView" />
        <activity android:name=".chapter6.UriActivity" />
        <activity android:name=".FragmentActivity" />
        <activity android:name=".MenuActivity" />
    </application>

</manifest>